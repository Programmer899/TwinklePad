#include "usb_hid.h"
#//include "tusb.h"

void usb_hid_init() {
    //tusb_init();
}

void usb_hid_send(uint8_t *keys) {
    // if (tud_hid_ready()) {
    //     tud_hid_keyboard_report(0, 0, keys);
    // }
}
